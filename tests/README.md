# Тесты интерфейса

Два скрипта на Playwright. Оба гоняют приложение в демо-режиме (без Supabase),
поэтому ничего не трогают в базе и не требуют доступа к серверу.

## Подготовка (один раз)

```bash
npm install playwright-core           # без браузеров
# бинарь Chromium указывается переменной PW_CHROME, если он не в стандартном месте
```

## Запуск

```bash
# демо-копия: те же файлы, но пустой config.js — приложение стартует на localStorage
rm -rf /tmp/tlrun && cp -r . /tmp/tlrun
echo 'window.TECHLOG_CONFIG = {};' > /tmp/tlrun/config.js
(cd /tmp/tlrun && python3 -m http.server 8099 &) && sleep 2

node tests/ui-check.js 8099            # интерфейс + прокрутка, все экраны; с 1.08.68 — плюс тест и учебник каждого раздела учёбы
node tests/books-overlap.js 8099       # все страницы всех учебников: пересечения текста с картинками, линиями и текстом (v1.08.68)
node tests/v1_08_70.js 8099            # перемешивание вариантов ответов: галочка админа, комбо-пункты → «отметьте все верные», разбор (v1.08.70)
node tests/v1_08_72.js 8099            # встроенный регресс основных функций (Настройки → Диагностика) на телефоне и ПК (v1.08.72)
node tests/v1_08_73.js 8099            # камера в приложении (фейковая камера Chromium), приёмник intake, «Поделиться → TechLog» через service worker (v1.08.73)
node tests/v1_08_74.js 8099            # «Тест съёмки: фото и видео» из настроек — сквозной прогон в демо с фейковой камерой, отчёт, копирование, .txt (v1.08.74)
node tests/v1_08_75.js 8099            # метрики отклика камеры: плашка, тап по затвору, отложенный разбор после «Готово», сводка и копирование, шаг в тесте съёмки (v1.08.75)
node tests/v1_08_76.js 8099            # журнал теста целиком: localStorage построчно, Скачать/Копировать/Показать, прерванный тест, регресс в журнале, «Журнал событий» .txt (v1.08.76)
node tests/v1_08_77.js 8099            # режим превью камеры (Быстрое/Максимум), режим и «снимок до» в метриках, экспорт метрик .txt, «нет тапов» в тесте (v1.08.77)
node tests/v1_08_78.js 8099            # серия через очередь затвора, кольцо ожидания, 480p, профиль телефона и вердикты по профилю (v1.08.78)
node tests/v1_08_79.js 8099            # Способ 1/2: одна кнопка «Фото», личная настройка, баннер и «Забрать кадры», intent камеры, «Поделиться» в открытый документ без вопроса (v1.08.79)
node tests/v1_08_80.js 8099            # журнал Способа 2, полуручной тест Способа 2 с имитацией ручных шагов, продолжение после перезагрузки, прерывание (v1.08.80)
node tests/v1_08_84.js 8099            # одиночный PDF-инвойс: альбомный Letter, бланк слева, лист-продолжение справа при длинном хвосте; настоящий jsPDF (JSPDF_UMD=…/jspdf.umd.min.js, иначе CDN) (v1.08.84)
node tests/v1_08_86.js 8099            # галочка «только при ошибке» — настройка аккаунта (профиль, кэш, перенос), место в «Всплывающих подсказках»; полоска отправки = тосты по центровке: сверху/снизу/сбоку, телефон и ПК (v1.08.86)
node tests/v1_08_87.js 8099            # кликабельные плашки главной: курсор-рука, Enter; модалка «Пикапы: сегодня и просроченные» с мини-карточками, числа = плашка, карточка → пикап → назад (v1.08.87)
node tests/v1_08_88.js 8099            # одна модалка «Проверка связи» из шапки и из настроек, «Копировать лог», строка домена (DNS Cloudflare) и мягкий вердикт по сайту cloudflare.com (v1.08.88)
node tests/v1_08_89.js 8099            # размер шрифта: свой у режима «Телефон» и «ПК», хранится в профиле аккаунта, перенос старого общего значения, описание в подсказке «?» (v1.08.89)
node tests/v1_08_90.js 8099            # мгновенная модалка диагностики со стримом строк, объяснение синхронизации, «Push уведомления» и кнопка «Проверить работу уведомлений» (v1.08.90)
node tests/v1_08_91.js 8099            # справка по нумерации под спойлером прямо в карточке: то же содержимое, что в окне «?», состояние помнится (v1.08.91)
node tests/v1_08_92.js 8099            # «Документы без перевода»: галочки, фильтры, открытие документа; 2FA: QR в белой рамке, калитка входа без кода, отмена входа (v1.08.92)
node tests/v1_08_93.js 8099            # окно кода 2FA не обойти: подложка и «назад» = «Отмена» с выходом, за окном приложения нет; обычные модалки закрываются как раньше (v1.08.93)
node tests/v1_08_94.js 8099            # Enter в поле кода 2FA = «Подтвердить» (включение, отключение, вход); в поле только цифры, вставка с пробелами чистится (v1.08.94)
node tests/v1_09_01.js 8099            # справочник «Трекеры Bouncie»: вкладка, счётчики и фильтр, сверка кнопкой/при открытии, пропал → «неактивен» без удаления, вернулся → активен, пустой список не трогает статусы, выбор трекера в карточке машины (занятые недоступны, VIN/марка подставляются), ⚠ у машины с неактивным трекером, карта; Leaflet-заглушка как у bouncie.js (v1.09.01)
node tests/v1_09_02.js 8099            # личные настройки меню: «Названия пунктов меню» (Авто/Показать/Скрыть, отдельно телефон и ПК), «Рядов меню на телефоне» 1…5 (поровну, ≥ 4 в ряду, --tbx поднимает экран/«+»), запись в профиль и перезагрузка, ПК-колонка и узкое окно, 2FA строкой под «Сменой пароля» (v1.09.02)
node tests/v1_09_03.js 8099            # замок правки старых задач: галочка вкл/выкл, срок от 1 дня (ноль не выставить), срок запоминается, строка «Сейчас: …», подсказка «?» висит дольше и закрывается нажатием; автобэкап: памятка трёх полок, список копий по группам с бейджами и счётчиками, предупреждение про старую функцию backup; телефон и ПК
node tests/v1_09_04.js 8099            # значок «копировать» — два листа; справка Главной/Доски: цвет левой полосы, пустые карточки-образцы (снимки в tests/out)
node tests/v1_09_05.js 8099            # компактная плотность: настройка по режимам и профиль, класс до старта app.js, геометрия «густого» дня и доски против обычной, ▲▼ по наведению, доска не под закреплённым меню, окно 1100 px; холст ПК-режима на телефоне (1100/1280/Выкл, подсказка, книжная ориентация, предел 45%, мышь, браузер без viewport); диагностика в компактном — 0 дефектов (v1.09.05)
node tests/v1_09_39.js 8099            # машины в одной точке — значки веером, номера видны; карта приложения и ТВ (v1.09.39)
node tests/v1_09_40.js 8099            # замечания по коду v1.09.38, пакет 1: долг по ремонту от суммы счёта, скрытые цены пропозала/ремонта и в PDF, удаление строки справочника со связями, статистика без архива, «Добавить комплекс» по ролям, недельная доска сотрудника, печать ремонта/пропозала, пуш «Пора на ТО», время журнала, очередь переноса PDF в архив; Edge и SQL — по исходникам; копии для Dashboard (v1.09.40)
# psql -tA -d <база> -f tests/v1_09_40.sql     # SQL: стражи удаления справочников и сотрудника (USER_HAS_DOCUMENTS), журнал (автор, время, роль, запреты по роли), «ничейный» пикап, media.upload_id
node tests/v1_09_41.js 8099            # замечания по коду v1.09.38, пакет 2: ремонт «только просмотр», справка «Ремонт»/«Моя машина», бригада везде, «Назад» документа, продления и «Ждут апрува», поиск без архива и номера по шаблону, права пропозалов/ремонта в «Настройках документов», «Блоки Note», номер машины через «Автомобили», своя колонка и лента недели на Доске, фильтры журнала, баннер пикапов, окно склада, цвета контрагентов (v1.09.41)
node tests/v1_09_42.js 8099            # замечания по коду v1.09.38, пакет 3: вопросы в окне приложения поверх открытого окна (Да/Esc/«назад»/ввод текста, подмена встроенного прогона), ни одного confirm()/prompt() в коде, размер файлов в настройках, мёртвый код, архив equipment_stock, подписи ТО mnt_*, роль в create table, свои версии функций (v1.09.42)
node tests/v1_09_38.js 8099            # пояс фирмы, рабочее время пушей, ТО/заметки/«Моя машина», машина из трекера, логотип, подгонка меню, чат, прокрутка, этапы водителя на ТВ и доске, офис и маршрут, правило PDF на Диске (v1.09.38)
# psql -tA -d <база> -f tests/v1_09_38.sql     # SQL: app_tz/app_today, окна рабочего времени (выходные, ночная смена, переход на зимнее), тик расписания, пуш «Пора на ТО», RLS заметок и справочника ТО
node tests/v1_09_06.js 8099            # кнопка «назад»: вкладки в обратном порядке и та же прокрутка, модалка/окно из окна/выпадающий список, документ с Доски → Доска с сохранением правок, поиск и редактор пропозала, подсказка о выходе и выход только на главном экране (бухгалтер, экран входа, ПК) — настоящим history.back() (v1.09.06)
node tests/ui-matrix.js 8099 --dens=compact   # та же матрица (26 устройств, с телефоном в ПК-режиме) в компактной плотности → tests/out/ui-matrix-compact.md
UI_DENS=compact node tests/ui-check.js 8099    # ui-check в компактной плотности
node tests/backup-rotation.js           # без браузера и сервера: чистые функции Edge Function backup (вид копии, ISO-неделя, план автобэкапа, ротация) — год ежедневных запусков: ежедневных ровно 8, недельные/ручные/старый формат целы; дубли дня; страховки в тексте функции. Нужен пакет typescript (NODE_PATH), без него — SKIP
node tests/repairs.js 8099             # документ ремонтных работ: сквозной сценарий
node tests/repairs-roles.js 8099       # он же: права работника, менеджера, админа
node tests/scroll-regress.js 8099      # перенос карточек, доска, режимы
node tests/stock.js 8099               # склад: регистр, большие кнопки, все операции
node tests/ui-matrix.js 8099 --shots   # 24 устройства: телефоны, планшеты, мониторы
node tests/tv.js 8099                  # режим телевизора: вход → экран ТВ, киоск, настройки и конструктор (v1.08.37)
node tests/v1_08_38.js 8099            # офлайн-режим (пилюля связи, блеклые серверные кнопки, задача без сети) и спойлеры инвойса
node tests/v1_08_39.js 8099            # бухгалтерия: роль, реестр, проценты, отметки, CSV, сводка по сотрудникам (v1.08.39)

node tests/unit.js                     # jsdom, без браузера: словарь, версии, регистр-математика
NODE_PATH=/tmp/vmtest/node_modules node tests/inv-pdf.js . long /tmp/inv.pdf   # jsdom + jspdf из npm: рендер одиночного инвойса в PDF для просмотра глазами (варианты short/mid/long/huge; pdftoppm -png) (v1.08.84)
node tests/v1_08_57.js 8099            # полоска отправки фото/видео: оформление, крестик, Esc, «подробнее», положение сверху/снизу/сбоку на телефоне и ПК (v1.08.57)
node tests/v1_08_51.js 8099            # учёба: вкладка, разделы, тест (обучение/экзамен), итоги, разбор, чтение, статистика, доступ (v1.08.51)
```

```bash
# БД (одна транзакция, в конце ROLLBACK — безопасно для любой базы):
psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/acc.sql   # бухгалтерия: роль, acc_doc_mark, триггер acc_guard, acc_settings, RLS (v1.08.39)
psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/study.sql # учёба: колонки доступа, profiles_guard v3, study_sessions (RLS свои/админ), admin_restore_rows (v1.08.51)
psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/bn-devices.sql # трекеры Bouncie: bn_devices_sync (новые / пропавшие → inactive без удаления / вернувшиеся / пустой список), разбор ответа, FK vehicles.imei, vehicle_save (NO_DEVICE / DEVICE_INACTIVE / DEVICE_TAKEN), RLS только админ (v1.09.01)
```

## `v1_08_51.js` и `study.sql` — учёба (v1.08.51)

`v1_08_51.js` (38 проверок, демо-режим): кнопка «Учёба»/«Study» в меню; экран — восемь
разделов, у 3/5/7 активен «Тест», у 8 — «Материалы»; модалка старта; тест раздела 3 в
режиме «Обучение» (варианты, таймер, черновик в localStorage, подсказка, проверка,
разбор, «Дальше» с выбранным ответом, восстановление после перезагрузки, «Прервать» →
итоги, запись сессии с ответами, разбор); «Экзамен» без разбора; чтение раздела 8
(рамка без скриптов, сессия read ≥ 5 с); «Мои результаты» и «Статистика» (сводка,
строка сотрудника, раскрытие); доступ — карточка настроек, «По списку», флаг
техника, личное скрытие, общий выключатель, ⚙️ сотрудника; справка «?».

`study.sql` (23 проверки): колонки и умолчания, `profiles_guard` v3 (техник меняет
`study_off`, `study_access` — FORBIDDEN_FIELD; админ ставит), `admin_restore_rows`
для `study_sessions` (вставка, дубль, kind вне списка), RLS под `authenticated`
(техник пишет и повторно апсертит свои строки, чужой user_id — отказ, читает только
свои, не удаляет; менеджер — только свои; админ — все, удаляет).


## `v1_08_39.js` и `acc.sql` — бухгалтерия (v1.08.39)

`v1_08_39.js` (демо, 50 проверок): вход бухгалтером — свой набор вкладок без
главной/доски/склада и бейдж роли; реестр раскладывает демо-инвойс по
категориям (клининг $295, аренда $630 = BLW $450 + DHM $180, итог $925 =
jobGrand), проценты сохраняются одной кнопкой (общий процент аренды и свой
на BLW → «К выплате» $226, название колонки, штамп автора, отказ для 140 %),
карта «секция → категория» (Other services → материалы и сброс), отметка с
заметкой и массовая отметка, фильтры и поиск, карточка только для чтения
вместо формы, сводка по сотрудникам (поровну ↔ основному), CSV (BOM, шапка,
итог), у админа — вкладка и строка «Бухгалтерия» в форме инвойса, у техника
вкладки нет и бухгалтера нет в выборе исполнителя; ПК-таблица на 10 колонок;
словарь ru/en.

`acc.sql` (43 проверки): роль accountant в constraint / admin_set_role /
admin_create_user, RPC `acc_doc_mark` для бухгалтера и админа (техник и
менеджер — FORBIDDEN; BAD_STATUS, BAD_KIND, NOT_FOUND, NOTE_TOO_LONG),
триггер `acc_guard` (чужой upsert полной строки отметку не затирает, апрув
не сбрасывается, пуш не уходит), `acc_settings` (штамп автора, 0…100,
восстановление через `admin_restore_rows`), RLS под `authenticated`
(бухгалтер читает чужие документы и пишет acc_settings, техник и менеджер
настроек не видят).

## `stock.js` — склад и регистр оборудования (v1.08.29)

Сквозной сценарий на экране «Склад» в демо-режиме (движения аренды в демо
зеркалит `demoPlMoves` — та же логика, что у серверного триггера): начальный
ввод → аренда с **авто-добором** → большие кнопки «Взять/Сдать» (чипы типов,
кнопка «всё · N», степпер, счётчики) → ремонт туда-обратно → поступление с
комментарием и списание (админ) → «забрал» → «Вернуть всё на склад» → отмена
вывоза → правка количества аренды → **бумажное** частичное продление →
справка «?» со схемой → экран техника без админских кнопок. ~36 проверок.

## `stock-register.sql` — сквозной автотест базы (все типы документов)

Не Playwright: чистый SQL по-настоящему выполняется в базе со схемой
v1.08.27+ и создаёт **все типы документов приложения** — пропозал, работу
(связь `link_job_proposal`), аренды двух типов оборудования, запросы
продления (отклонение и одобрение через `decide_ext_request` — полное и
частичное), «забрал»/«вернул» с отменами, правку количества, документ
ремонтных работ с апрувом, заявку на код доступа, все семь операций регистра
и удаление работы с каскадом, плюс блок **«профили и роли»**: создание
сотрудника админом, назначение всех трёх ролей, блокировка/разблокировка,
правка профиля и регрессия бага «админ не может изменить роль»
(клиентский RLS-upsert). После каждого шага сверяются остатки
(`equip_bal`, `stock_counts`), в конце — инвариант сохранения журнала и
счётчик аудита. **Безопасен для любой базы**: одна транзакция, в конце
`ROLLBACK` — не остаётся ни строки, даже в `audit_log`.

```bash
psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/stock-register.sql
psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/tv.sql   # v1.08.37: сессии ТВ, апрув, фид, отзыв (BEGIN…ROLLBACK)
```

Или вставьте файл целиком в Supabase SQL Editor: строки `ok …` появятся в
выводе, успех — `СКЛАД-РЕГИСТР: все N проверок пройдены`. Любое расхождение
останавливает скрипт ошибкой с именем шага.

Сервер должен подниматься **в том же вызове оболочки**, что и тест: фоновые
процессы между вызовами не переживают.

## `ui-check.js` — интерфейс и прокрутка

Дергает тот же движок, что и кнопка «Диагностика интерфейса» в приложении
(`window.UIDiag`), по всем экранам и в двух режимах — телефон 414×896 и ПК
1440×900. Валит сборку, если:

* появился **блокирующий** (не `passive`) слушатель `wheel` / `touchmove`
  на `window` или `document` — из-за таких прокрутка ждёт главный поток
  на каждом экране приложения. В отчёте печатается файл и строка, где
  слушатель зарегистрирован;
* сработала любая «жёсткая» проверка: перекрытие интерактивных элементов,
  налезание соседних блоков, вылет за правый край, кнопки, недоступные под
  панелями, битые `onclick="App.xxx()"`, дубли `id`, просадки кадров.

Замечания об оформлении (контраст, размер зон нажатия, обрезанный текст)
печатаются, но сборку не валят — они в списке `SOFT`. Чтобы валили тоже:

```bash
UI_STRICT=1 node tests/ui-check.js 8099
```

С v1.08.68 после экранов идёт блок «учёба»: для каждого раздела 1–8 —
карточка, окно запуска теста, первый вопрос с ответом и проверкой, учебник
в рамке (просмотрщик отрисовал страницы, тулбар и страница в границах рамки,
оглавление не пустое). Строка отчёта: `учёба/3 дефектов 0 · тест · книга 54 стр.`

## `books-overlap.js` — вёрстка учебников: пересечения

Пробегает по всем страницам всех учебников из `dictionary/index.json`
(RU и EN, 1172 страницы ≈ 20 с), отрисовывает каждую в настоящем
просмотрщике и меряет фактические прямоугольники элементов SVG. Ищет:

* **текст ∩ картинка** — надпись легла на фото или схему;
* **текст ∩ линия** — линейка, рамка таблицы или стрелка проходит сквозь
  буквы (подчёркивания под базовой линией не считаются; склеенные пути
  разбираются на подпути, белые заливки-«ластики» пропускаются);
* **текст ∩ текст** — два фрагмента наехали друг на друга (одиночные знаки
  препинания из отдельного шрифта не считаются);
* **край** — текст вышел за пределы страницы.

В консоль — итог по книгам и первые находки, полный список (страница,
вид, координаты, текст) — `tests/out/books-overlap.txt`. Находки — это
дефекты исходной вёрстки PDF (проверено по оригиналам), конвертер их
переносит один в один; править нужно исходный документ и пересобрать книгу.

```bash
BOOKS=section-6-ru node tests/books-overlap.js 8099    # только одна книга
PAGES=1-20 node tests/books-overlap.js 8099            # только диапазон физических страниц
BOOKS_STRICT=0 node tests/books-overlap.js 8099        # не валить сборку, только отчёт
```

## `scroll-regress.js` — что могло сломаться от правок прокрутки

С v1.07.67 `wheel` вешается на сам `.board` при отрисовке, а `touchmove` —
на перетаскиваемые карточки главной. Тест сторожит именно эти места:
привязку обработчиков после `render()`, перенос карточки пальцем через
long-press, горизонтальную прокрутку доски колесом и уборку ПК-надстроек
при возврате в мобильный режим.

## `ui-matrix.js` — разные экраны и разрешения

Открывает приложение в размерах настоящих устройств и на каждом обходит все
экраны тем же движком. Набор: iPhone SE / 13 mini / 14 / 16 Pro / 15 Plus /
16 Pro Max, узкий Android 360, Pixel 7, минимальные 320px, iPhone в альбомной,
iPad mini / 10.9 / Pro 12.9 книжной и альбомной, ноутбуки 1280 и 1366,
MacBook 1440, Full HD, 2K и ultrawide 3440 — в обоих режимах приложения там,
где это имеет смысл.

Итог складывается в `tests/out/`:

* `ui-matrix.md` — читаемый отчёт. Замечания сгруппированы **по сути**, а не по
  устройству: одна и та же беда на восьми телефонах — это одна запись со
  списком устройств, а не восемь одинаковых абзацев;
* `ui-matrix.json` — то же машинно, удобно сравнивать выпуски;
* `shots/*.png` — снимки главной, доски и настроек на каждом устройстве
  (только с ключом `--shots`).

Полезные ключи:

```bash
node tests/ui-matrix.js 8099 --only=iPhone,iPad     # часть набора
node tests/ui-matrix.js 8099 --screens=home,settings # часть экранов
```

Чего эмулятор **не** покажет: вырез и домашнюю полосу iPhone
(`env(safe-area-inset-*)` там всегда 0), настоящую инерцию прокрутки iOS и
системный масштаб шрифта. Это проверяет кнопка диагностики на живом
устройстве — она пишет в отчёт размер экрана, плотность точек, ориентацию,
безопасные поля и режим, так что присланный `.txt` читается однозначно.

## Порог

Если понадобится править движок проверок — он весь в `uidiag.js`, в одном
файле с кнопкой и отчётом. Тесты новых проверок не требуют: они читают
`window.UIDiag.json()` и ориентируются на уровень (`err` / `warn` / `ok`).


## unit.js — логика без браузера (v1.08.08)
`npm i jsdom@24` и `node tests/unit.js`. 216 проверок за ~3 секунды: словарь ru/en,
нумерация документов, двуязычные заметки и транслит, права на перевод, архив,
съёмка, контракты edge-функций и SQL-файлов. Браузер не нужен.

## offline.js — обрывы связи (v1.08.08)
`node tests/offline.js <порт>` при поднятом сервере. Проверяет запуск без сети из
кэша service worker, переходы по экранам в офлайне, сохранность правки после
перезагрузки без сети, доступность очереди фото и возврат сети.

## chain.js — цепочка документов (v1.08.11)
`node tests/chain.js <порт>` при поднятом сервере. Строит сценарии полного и
частичного продления, проверяет вызов цепочки из работы и из пропозала,
порядок карточек, метки «вы здесь» и «закрыт продлением», дни на стрелках и
переключение линии с горизонтальной на вертикальную.

- `v1_08_38.js` — офлайн-режим и спойлеры инвойса: пилюля связи под логотипом (зелёная «NN мс» → красная «офлайн» при `context.setOffline(true)`), пометка серверных кнопок `.net-need`, перехват клика подсказкой без вызова обработчика, создание задачи без сети, возврат сети снимает блокировку; форма работы — пустые секции свёрнуты, тап по заголовку, «?» не сворачивает, «Развернуть все (N)» ↔ «Свернуть пустые», ручной выбор переживает перерисовку, ПК-бланк с подписью DESCRIPTION. Демо-режим, ~43 проверки.
- `unit.js` с v1.08.38 дополнительно поднимает второй jsdom **в режиме Supabase на заглушке клиента** и проверяет офлайн-запись: без сервера `dbUpsert`/`dbDelete`/`dbSaveOrg`/`audit` не делают ни одного вызова и кладут строки в `techlog_pending` (включая `insert` журнала и `org_settings`), `pendingFlush` досылает всё одним проходом, сетевая ошибка «на ходу» оставляет запись в очереди.
- `v1_08_33.js` и `bouncie.js` читают Leaflet-заглушку из `/tmp/pw/leaflet-stub.js` (в репозиторий не входит): она должна не только отдавать цепочечные no-op (`L.map().attributionControl.setPrefix`, `L.control.attribution()`), но и **рисовать маркеры в DOM** контейнера карты — `<div class="pw-marker" data-pop="…">{divIcon.html}</div>` и `<div class="pw-line dash">` для полилиний с `dashArray`, иначе проверки карты Bouncie падают на `.map-car`.
- `v1_08_33.js` — пуши/2FA в настройках, глобальный поиск (свои активны, чужие «замком»), отчёт «Время», оптимизация маршрута, «Создать такую же», ⚙-доступы и сессии в Штате, значки машин (Check Engine / ТО / трек). Демо-режим, порт как у остальных.
- `v1_08_39.js` — бухгалтерия: роль «Бухгалтер» (свои вкладки, редирект главной, бейдж, форма инвойса не открывается — карточка только для чтения), реестр (раскладка демо-инвойса $295 / $630 с BLW $450 · DHM $180 / $925), проценты (общий процент аренды + свой на BLW → «К выплате» $226, отклонение 140 %), карта «секция → категория» и сброс, отметки и заметка бухгалтера (одиночная, массовая, фильтры), поиск, сводка по сотрудникам (поровну / всё основному), CSV с BOM, вкладка у админа и её отсутствие у техника, бухгалтер вне выбора исполнителя, ПК-таблица на 10 колонок, полнота словаря acc_*. Демо-режим, ~50 проверок.
- `acc.sql` — сквозной сценарий бухгалтерии на живой базе (BEGIN…ROLLBACK, схема v1.08.39): роль accountant в constraint / `admin_set_role` / `admin_create_user`, `acc_doc_mark` (бухгалтер и админ ставят отметки на инвойс и REP; техник и менеджер — FORBIDDEN; BAD_STATUS / BAD_KIND / NOT_FOUND / NOTE_TOO_LONG), триггер `acc_guard` (полная строка техника учётные поля не затирает, апрув не сбрасывается, пуши не уходят), `acc_settings` (штамп автора, check 0…100, восстановление через `admin_restore_rows`), RLS под ролью `authenticated` (бухгалтер читает чужие документы и пишет `acc_settings`, чужой инвойс не пишет; техник и менеджер `acc_settings` не видят). Запуск: `psql "$DB_URL" -v ON_ERROR_STOP=1 -f tests/acc.sql`, ~43 проверки.
- `unit.js` с v1.08.39 проверяет раскладку по категориям (`accSplitJob` / `accSplitRep`), проценты и переопределение по типу техники, пропорцию при апруве, доп. работы (покупка → материалы, флаг «ремонт» → ремонт, карта extra), реестр с REP, итоги и сводку по сотрудникам, фильтры и `viewAcc`.

## v1.09.25 · документооборот инвойса

- `v1_09_25.js` — браузер, демо-режим (46 проверок): сокращения и личные права в списке сотрудников, черновик без номера, «Выполнено» выдаёт и замораживает номер, режим просмотра (поля не нажимаются, фото добавлять можно), помощник и «Общий доступ», смена основного ☆, отзыв, возврат на доработку с причиной, апрув, запрос правки (отказ и разрешение), окно правки на сутки, «сняли с задачи», лента «Уведомления», «Важные объявления», окно конфликта. Запуск: `node tests/v1_09_25.js <порт>`.
- `docflow-sb.js` — jsdom, режим Supabase на заглушке клиента (16 проверок): ревизия читается тем же запросом, что и запись; `STALE_DOC` → окно конфликта и «записать мои поверх» со свежей ревизией; служебная правка (приоритет) при устаревшей строке накатывается на свежую сама; `DOC_LOCKED_*` — строка возвращается к серверной, в очереди не остаётся; мягкое «занято» (`doc_lock` / `doc_unlock`); заморозка номера функцией `job_fix_no`; поведение до выполнения SQL. Запуск: `NODE_PATH=<jsdom@24>/node_modules node tests/docflow-sb.js`.
- `docflow.sql` — база (84 проверки, BEGIN…ROLLBACK): видимость для бригады, личное право помощника, ревизия, замки «Выполнена» и «Апрув», отзыв и возврат, апрув по личному праву, заморозка номера, запрос правки и окно правки при запрете по давности, смена основного и бригады (включая старый баг «менеджер не может назначить исполнителя ничейной задаче»), лента при выключенном пуше, «занято», вставка сразу «Выполнена», круг «Важных объявлений», заморозка номеров пачкой. Запуск: `psql -tA -d <база> -f tests/docflow.sql` → «ИТОГ: 84 ✓ / 0 ✗».
- В `acc.sql` и `stock-register.sql` исправлена «гниль» самих тестов, из-за которой они падали ещё на 1.09.24: пароль создаваемого сотрудника короче 10 знаков (правило 1.09.24) и трекер не из справочника (правило 1.09.01). В `acc.sql` шаг «техник правит инвойс» идёт по новому правилу: сначала отзыв из «Выполнена», потом сдача. `bn-devices.sql` падает с `CAR_NO_TAKEN` и на чистой 1.09.24 — не связано с этим выпуском.
- В `v1_09_17.js` первый человек в списке переписок ищется не по номеру строки: системных разделов стало три (добавилась лента «Уведомления»).

## v1.09.26 · документооборот, исправления по разбору

- `v1_09_26.js` — браузер, демо-режим (29 проверок): пункт меню «Документооборот» со счётчиком; «Выполнено» без перевода → окно «Нужен перевод» → «Сформировать переводы» → документ ушёл на согласование; пометки DRAFT / APPROVED в PDF; своя заметка пикапа (инвойс не тронут); отозванный документ с выданным номером не удаляется; техника убрана → пикап в архиве с пояснением, та же техника ставится заново; менеджер технику в чужом документе только видит; апрув своего инвойса — по настройке админа; привязка пропозала к запертому инвойсу — подсказка с названием настройки; вопрос про апрувленную сумму; отклонённая версия в «Документообороте» (открыть · убрать), кликабельные строки и подсказка; новые галочки админа; документы заблокированного сотрудника.
- `docflow.sql` — теперь 108 проверок: к прежним добавлены перевод при отправке, техника у менеджера, устройства одного человека, удаление только «ни разу не отправленного», привязка пропозала, апрув своего инвойса, авто-закрытие запроса, уведомление бухгалтеру, поля пикапа.
- `stock-register.sql` — +4 проверки: пикап в архив возвращает технику в регистре, возврат из архива снова её списывает.
- В `v1_09_12.js` проверка кнопки «Перенести день» заменена на «выключен совсем»; в `acc.sql` заметке добавлен перевод (без него документ на согласование не уходит).

## v1.09.27 · тест документооборота

- `v1_09_27.js` — браузер, демо-режим (27 проверок): режим включает только админ и на срок, предупреждение админу при входе, карточка скрыта у остальных при выключенном режиме; **весь встроенный сценарий прогоняется за три роли** (работник 64 шага, менеджер и админ по ~60) — без провалов, обязательные шаги роли не пропущены, негативные шаги получили именно свои отказы, после теста ничего тестового не осталось; «Пошагово», «Остановить», «Убрать остатки тестов». В демо правила сервера повторяет `dftDemoExec`.
- `dft.sql` — база (34 проверки): пометку «тестовый» клиент не ставит; `dft_exec` недоступна клиенту; режим — только админ, срок ≤ 72 ч, журнал; правила действуют под подменённой личностью; **настоящий документ функция не трогает** (апрув, правка, пикап, привязка настоящего пропозала); чужой прогон; закрытые списки операций и функций; заблокированные; тестовые пикап и пропозал, склад и настоящая нумерация не тронуты; события только владельцу прогона и без пушей; срок; уборка при выключенном режиме.
- `dft-fn.js` — Edge Function `dft` офлайн (15 проверок): вход, блокировка, `DFT_OFF`, срок, уборка при выключенном режиме, журнал запуска, выбор исполнителя по роли (админ, включивший режим; менеджер без права / с правом; только настоящий работник), закрытый список операций, отказ базы как ответ HTTP 200. Нужен `typescript`.

## v1.09.28 · тест документооборота кнопками

- `v1_09_27.js` — теперь 29 проверок: сценарий за три роли идёт КНОПКАМИ приложения (работник 64 шага, менеджер и админ по 60; в отчёте сотни «☛ нажал» и «⌨ ввёл»), служебно — только шаги чужих ролей, приём документа в тест (`job_adopt`) и «запросы в обход интерфейса»; все 16 кодов отказа сервера встречаются хотя бы в одном прогоне.
- `dft.sql` — 42 проверки: + `job_adopt` (свой свежий черновик DFTEST принимается; рабочий, старый, чужой, сданный — `DFT_ADOPT_DENIED`), + номер пикапа после вставки (тестовый — из тестового диапазона, настоящий счётчик и склад не тронуты; повторный upsert счётчик не тратит).
- `docflow.sql` — 114 проверок: + техника в собственном документе менеджера, `BAD_STATUS`, `BAD_TOTAL`, `NOT_FOUND`, авто-закрытие запроса при удалении документа, разовый перенос общей галочки апрува в личные права.

## v1.09.29 · тест документооборота: новые сценарии и журнал

- `v1_09_27.js` — 35 проверок: сценарий кнопками за ЧЕТЫРЕ роли (работник 74 шага, менеджер, менеджер с правом апрува, админ), новые сценарии (кнопка перевода, разделы «Документооборота», «Записать заново», заявка на продление, частичное продление, помощник забирает, PDF DRAFT, лента согласующего, обе позиции настроек и личное право помощника у админа), состав отчёта (шапка, ∑-снимки, 💬, ▣, ?, итог по ленте и журналу событий).
- `docflow.sql` — 119 проверок: + ревизия на момент апрува (`approved_rev`): ставится при апруве, правка после апрува видна как `rev > approved_rev`, обновление суммы апрува переставляет её, клиент её не меняет, снятие апрува очищает.

## v1.09.30 · тест документооборота: все ветки и все роли

- `v1_09_27.js` — 38 проверок, четыре роли: работник 89 шагов, менеджер 78, менеджер с правом апрува 82, админ 90. Новое: второй круг согласования от имени ДРУГОГО согласующего; документ ремонта (создан кнопками, апрув менеджером, отклонение и апрув админом, запреты); удаление инвойса со связанным ремонтом; экран «Архив» (восстановление, «Удалить навсегда»); запрет правки по давности и окно правки; пропозал кнопками; лента «перенесена» / «передана вам»; штамп APPROVED.
- `dft.sql` — 53 проверки: + тестовый ремонт (создание, апрув менеджером с правом, отклонение админом, отказы работнику и менеджеру без права, ремонт к настоящей задаче не создаётся), + пометку у ремонта клиент не ставит, + `prop_adopt` / `rep_adopt`, + уборка тестовых ремонтов.

## v1.09.31 · пропозал без «отправки клиенту»; фото/видео/PDF на Диск, цикл пропозала и пуши в тесте

- `v1_09_27.js` — 43 проверки, четыре роли (работник 95 шагов, менеджер 83, менеджер с правом апрува 87, админ 96). Браузер запускается с поддельной камерой (`--use-fake-device-for-media-stream`): съёмка способом 1 идёт по-настоящему, отправка на Диск в демо помечается «пропущено». Новое: статус «Отправлен» у пропозала выключен и включается админом; цикл пропозала; очередь фото/видео после теста пуста.
- `dft.sql` — 58 проверок: + пуш тестового документа идёт ведущему тест (и только ему), с `test_wide` — всем участникам; `dft_pushes`; `PROP_SEND_OFF` и включение настройки.

## v1.09.32 · по итогам первого живого прогона

- `docflow.sql` — 132 проверки: + сохранение ЧЕРЕЗ UPSERT под настоящими политиками (роль `authenticated`): помощник с «Общим доступом» сохраняет документ основного; без доступа и посторонний — нет; помощник не создаёт чужой новый документ; админ отклоняет и менеджер с правом апрувит ремонт работника; посторонний работник — нет; «тестовая» строка не от функции — `DFT_TEST_ROW`.
- `dft.sql` — 58 проверок (ожидания обновлены: вставка «тестовой» строки клиентом отклоняется).
- `v1_09_27.js` — работник 99 шагов: + группа «Я — помощник в чужом документе».
- Урок: проверки доступа одним `UPDATE` недостаточны — приложение пишет `upsert`, а он проверяет ещё и политику вставки.

## v1.09.33 · по итогам второго живого прогона

- `dft.sql` — 63 проверки: + `ext_req_create` (без ожидающих пикапов — `DFT_NO_PENDING`, посторонний — `RLS_DENIED`, состав заявки), решение через закрытый список, уборка заявок.
- `v1_09_27.js` — заявка на продление при любой ведущей роли (подана, отклонена, новая одобрена с продлением).
- `unit.js` — 1068: подсказки из памяти теста, перехват fetch для функций, демо-зеркало заявки на продление.

## v1.09.34 · по итогам третьего живого прогона

- `dft.sql` — 65 проверок: + автора и время апрува ремонта записал сервер; повторная отправка снимает решение.
- `v1_09_27.js` — решение по заявке на продление ищется на экране «Доска».

## v1.09.35 · проблемы прогона по важности

- `v1_09_27.js` — после каждой роли: критических проблем нет, каждый пропущенный шаг есть в предупреждениях, отказы негативных шагов — «ожидаемые», кнопки выгрузки по важности на месте, в отчёте раздел «Проблемы прогона».
- `unit.js` — 1077: классификатор (негативный шаг → ожидаемый отказ, позитивный → ошибка, склейка ×N), кнопки выгрузки, подстановка сумм в вопрос про апрув, защита от повторной досылки записи в пути, повторный проход отправки фото, PDF не «фото», пауза трекера.

## v1.09.36 · функции сервера

- `unit.js` — 1080: проверка функции с подменённым `fetch` — свежая, старая версия, 404, «нет ответа без CORS» (обычный запрос падает, no-cors отвечает), нет связи, перепутан код; список проверки совпадает с `supabase/functions`.

## v1.09.37 · диагноз функций со стороны сервера

- `unit.js` — 1084: диагноз при «нет ответа без CORS» — 404 NOT_FOUND / BOOT_ERROR / «серверу отвечает, браузеру без CORS» / без dft 1.09.37; словарь сам проверяется на тексты с повторяющейся подстановкой, заполняемые одиночной заменой.
- `dft-fn.js` — 18: `probe` — работнику 403, менеджеру только функции проекта, ответ шлюза как есть, вход вызывающего передан, работает при выключенном режиме.

