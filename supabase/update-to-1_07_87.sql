-- ============================================================
-- TechLog · update to v1.07.87
-- Инвойсы можно разложить по сотрудникам: галочка в настройках
-- Google Drive. Включена — путь становится
--   <папка инвойсов> / Имя Ф / ГГГГ-ММ / файл.pdf
-- вместо <папка инвойсов> / ГГГГ-ММ / файл.pdf.
-- Имя папки берётся из профиля сотрудника: имя и первая буква
-- фамилии, латиницей — как подпись исполнителя в документах.
-- Идемпотентно: повторный запуск безопасен, данные не трогаются.
-- ============================================================
alter table public.org_settings
  add column if not exists gd_inv_by_tech boolean not null default false;

-- строка настроек существует и на старых базах
insert into public.org_settings (id) values ('org') on conflict (id) do nothing;
